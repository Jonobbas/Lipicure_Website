import type { MetadataRoute } from 'next'; import { contentItems } from '@/lib/content';
export default function sitemap():MetadataRoute.Sitemap{const base='https://lipicure.com'; const staticRoutes=['','/about','/technology','/marine-bioprocess-intelligence','/research','/team','/contact','/collaboration','/founder-story','/knowledge','/insights','/founders-desk','/news','/philosophy']; const dynamic=contentItems.map(i=>({url:`${base}/${i.kind==='insight'?'insights':i.kind==='founder'?'founders-desk':'news'}/${i.slug}`,lastModified:new Date(i.date)})); return [...staticRoutes.map(route=>({url:base+route,lastModified:new Date()})),...dynamic];}
